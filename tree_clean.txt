SCP tree (clean) @ 2026-03-04 14:48:37
?? amplify
  ?? data
    ?? InventorySnapshot
    ?? RepriceSnapshot
    ?? SalesSummarySnapshot
  ?? functions
    ?? inventory-backstop
    ?? new-lines-scan
    ?? schedulerTick
  ?? lib
?? app
  ?? api
    ?? clean
      ?? all-listings
        ?? ingest
      ?? drilldown
      ?? scheduler
        ?? tick
      ?? snapshot
      ?? stranded
        ?? ingest
    ?? cost
      ?? add
      ?? rollup
    ?? fees
      ?? estimate
    ?? hello
    ?? inventory
      ?? availability
      ?? delete_scheduler
        ?? tick
      ?? ingest
      ?? sku
        ?? upsert
      ?? snapshot
        ?? eu-total
        ?? latest
    ?? listings
      ?? truth
        ?? get
        ?? refresh
    ?? new-lines
      ?? has-inventory
      ?? scan
    ?? purchase-orders
      ?? active
      ?? csv
      ?? delete
      ?? draft
      ?? finalize
      ?? options
      ?? upsert-line
    ?? reports
      ?? scheduler
        ?? tick
      ?? suppliermap-audit
    ?? repricer
      ?? assignments
        ?? export
      ?? lookups
      ?? patch
      ?? run
      ?? status
      ?? strategies
    ?? restock
      ?? options
      ?? snapshot
      ?? table
    ?? sales
      ?? cancel-sweep
      ?? combined-snapshot
      ?? manual
        ?? ingest-tax
      ?? reports
        ?? cancellations
          ?? download
        ?? diagnose
        ?? fba-invoicing
          ?? download
        ?? orders
          ?? download
      ?? report-sync
      ?? scheduler
        ?? tick
      ?? sku-analysis
      ?? snapshot
        ?? latest
    ?? scheduler
      ?? tick
    ?? settings
      ?? app
    ?? spapi
      ?? ping
    ?? suppliermap
      ?? preview
      ?? upload
    ?? viewer
      ?? heartbeat
  ?? mi
    ?? clean
    ?? reports
    ?? restock
    ?? sales
  ?? new-products
    ?? investigate
    ?? list
    ?? search
  ?? orders
    ?? management
  ?? repricer
    ?? mapper
    ?? status
    ?? strategies
  ?? settings
    ?? uploads
    ?? user
?? components
  ?? cost
  ?? dock
  ?? pages
  ?? shell
  ?? viewer
?? lib
  ?? spapi
?? public
?? .env.local
?? amplify_outputs.json
?? eslint.config.mjs
?? next.config.ts
?? package.json
?? postcss.config.mjs
?? tsconfig.json
